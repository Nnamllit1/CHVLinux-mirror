# CHVLinux Mirror

This private repository is reserved for the future CHVLinux static APT mirror.

It is intentionally empty for now. The roadmap repository defines the package and mirror architecture before packages are published here.

Expected future contents:

- `dists/`
- `pool/`
- repository metadata generated by `reprepro` or equivalent tooling
- signed Release metadata
- CHVLinux archive public key

